{
  "name": "deploy-extension",
  "displayName": "Deploy Automático",
  "description": "Painel simples para automatizar push para GitHub e deploy no Vercel a partir do VS Code.",
  "version": "1.0.0",
  "publisher": "ryantirabassi",
  "engines": {
    "vscode": "^1.75.0"
  },
  "activationEvents": [
    "onCommand:deploy-extension.deploy"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",

    "vscode:prepublish": "npm run compile",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RyanTirabassi/Deploy_ProjetoIntegrador.git"
  },
  "contributes": {
    "commands": [
      {
        "command": "deploy-extension.deploy",
        "title": "🚀 Deploy Automático"
      },
      {
        "command": "deploy-extension.clearVercelToken",
        "title": "🧹 Limpar Token do Vercel"
      },
      { "command": "devopsExtension.loginGitHub", "title": "Login GitHub" },
      { "command": "devopsExtension.loginVercel", "title": "Login Vercel" },
      { "command": "devopsExtension.testApis", "title": "Testar APIs" }
    ]
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.75.0",
    "typescript": "^5.6.0"
  },
  "keywords": [
    "vscode",
    "deploy",
    "vercel",
    "github"
  ],
  "license": "MIT",
  "dependencies": {
    "vite-projeto": "file:.."
  }
}
